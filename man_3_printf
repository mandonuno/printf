.TH man 2 "28 February 2018" "0.32" "_printf man page"
.SH NAME
.B _printf
 An implementation of printf for Holberton School project.
.SH SYNOPSIS
_printf() should reproduce all functionality of the printf function.
#include "holberton.h"

int printf(const char *format, ...);

.SH DESCRIPTION
The functions in the _printf() function will produce output according to a specified format. _printf() write output to stdout.
.B Return value

Upon successful return, the function returns the number of characters printed. If an error is encountered, a negative value is returned.

.SH OPTIONS
.B d, i

The int argument is converted to signed decimal notation.

.B c

The int argument is converted to an unsigned char, and the resulting character is written.

.B s

The const char * argument is expected to be a pointer to an array of characters Characters from the array are writtten to stdout.

.B b

_printf() takes in an unsigned int and will return the value in binary.

.SH SEE ALSO
.I printf(3), va_arg(3)

.SH BUGS
No known bugs

.SH AUTHOR
Austin Harshberger, Armando Nuno